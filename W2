W2 Cheet sheet

git commit -a : stage files automatically
git show commitID: shows the commit about commit ID
git diff: shows the difference in commits
git add -p: add patch
git mv: moves a file
git rm: delete a file

.gitignore file: 
.gitignore files are used to tell the git tool to intentionally ignore some files in a given Git repository. 
For example, this can be useful for configuration files or metadata files that a user may not want to check into the master branch. 
Check out more at: https://git-scm.com/docs/gitignore.

$ git branch name : create branch
$ git branch checkout branch_name : switch to branch
$ git checkout -b branch_name: create and switch to a branch
$ git merge branch : merge two branches
$ git merge --abort: abort merge action when ther is a conflict

<Explore repo>
$ cd ~/food-scripts
$ ./food_count.py <- can see the contents of the python script
$ git checkout -b imrpove-output
$ git revert [commit-id]

$ git checkout master
$ git merge improve-output
$ git status
$ git log
